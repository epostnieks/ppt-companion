import AboutAuthor from '../../src/AboutAuthor';

export const metadata = {
  title: "About Erik Postnieks — Independent Researcher | System Asset Pricing Model Program",
  description: "Erik Postnieks, Independent Researcher, Salt Lake City.",
};

export default function AboutPage() {
  return <AboutAuthor />;
}
