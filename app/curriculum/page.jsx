import CurriculumClient from './curriculum-client';

export const metadata = {
  title: "System Asset Pricing Model Curriculum — Expanded Core | System Asset Pricing Model Program",
  description: "The expanded System Asset Pricing Model curriculum: theorem foundations, Decision Accounting, public policy, global jurisdiction, domain papers, source packages, and publication strategy.",
};

export default function CurriculumPage() {
  return <CurriculumClient />;
}
